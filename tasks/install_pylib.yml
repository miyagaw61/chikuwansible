- name: Install Basic Libraries By Pip
  shell: bash -lc "{{python3_binary}} -m pip install -U {{item.name}}"
  with_items:
    - name: "git+https://github.com/miyase256/enert"
    - name: "beautifulsoup4"
